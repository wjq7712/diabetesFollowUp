@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("注销", "LogOff", "Home") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "Index", "Home") ]
}
